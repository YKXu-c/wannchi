.SUFFIXES :
.SUFFIXES : .o .f90

.f90.o:
	$(F90) $(F90FLAGS) -c $(INCLUDES) $<

F90=gfortran
F90FLAGS= -O2 -cpp -fallow-argument-mismatch

INCLUDES=-I../modules -I.

LAPACKLIBS=-framework Accelerate

AR             = ar
ARFLAGS        = ruv

RANLIB         = ranlib
